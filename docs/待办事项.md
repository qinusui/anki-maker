# 待办事项

## 打包成 exe（进行中）

### 已完成
1. 前端构建成功（`npm run build`）
2. 创建 PyInstaller spec 文件（`anki-maker.spec`）
3. 修复 `backend/main.py` 路径问题：添加 `BASE_DIR` 变量处理 PyInstaller 打包环境
4. 修复 `backend/api/process.py` 导入问题：将 `importlib` 动态加载改为直接导入
5. 修复 uvicorn 无限循环重启：打包环境禁用 `reload=True`
6. 添加打包产物到 `.gitignore`

### 未完成 / 待继续
- **重新打包测试**：修改了 `main.py` 和 `process.py` 后尚未重新打包测试
- 命令：`.venv/Scripts/pyinstaller anki-maker.spec --clean -y --distpath dist3`

### 打包注意事项
- 必须使用虚拟环境的 PyInstaller：`.venv/Scripts/pyinstaller`
- 系统 Python 没有安装项目依赖（openai 等）
- `dist2/` 目录被占用无法删除，使用 `dist3/` 输出

### 修改的文件
| 文件 | 修改内容 |
|------|----------|
| `backend/main.py` | 添加 `BASE_DIR`、`sys.frozen` 检测、uvicorn reload 禁用 |
| `backend/api/process.py` | 移除 `importlib` 动态加载，改为直接导入；TEMP_DIR 路径修复 |
| `anki-maker.spec` | PyInstaller 配置文件（新建） |
| `.gitignore` | 添加 dist/ build/ *.spec |
